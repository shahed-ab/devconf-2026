FAQ Section

Centered section heading ("Frequently Asked Questions") with a subtitle below it
A light gray background to separate it visually from the section above
A vertical list of at least 5 collapsible question boxes (click to expand/collapse, no JavaScript — use HTML's built-in <details>/<summary> elements)
Each box must include:

A small arrow icon that rotates 90° when the box is opened
The question text, bold
A hidden answer paragraph that only appears when the box is expanded


Boxes should have:

Rounded corners and a soft drop shadow (not a flat plain border)
A hover effect that highlights the border color
The question text should change color when that specific box is open


The whole list should be centered on the page with a max width (don't let it stretch edge-to-edge on large screens)
Keep the code simple — plain CSS classes, no CSS variables or advanced selectors, so it stays beginner-friendly